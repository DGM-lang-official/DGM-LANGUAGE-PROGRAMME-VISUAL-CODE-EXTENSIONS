class Animal {
  fn speak() {
    return "noise"
  }
}

class Dog extends Animal {
  fn speak() {
    return "woof"
  }
}

let dog = new Dog()
writ(dog.speak())

let prefix = "good "
fn make_message(suffix) {
  return lam() => prefix + suffix
}
let message = make_message("dog")
writ(message())
